export const dynamic = "force-dynamic";
import SearchClient from "@/components/SearchClient";

export const metadata = { title: "Search Anime" };

export default function SearchPage() {
  return <SearchClient />;
}
